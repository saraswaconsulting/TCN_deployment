# Streamlit Cloud Requirements for Real-time ISL Recognition
# Python 3.10 compatible dependencies
# Optimized for live camera streaming with minimal dependencies

streamlit>=1.28.0
streamlit-webrtc>=0.63.0
# Pin to CPU wheels compatible with Python 3.10 on Streamlit Cloud
torch==2.0.1
torchvision==0.15.2
# Force headless-only OpenCV and a stable mediapipe pairing to avoid libGL
opencv-python-headless==4.11.0.86
mediapipe==0.10.9
google-generativeai>=0.3.0,<0.4.0
numpy>=1.24.0,<1.27.0
protobuf>=3.20.0,<4.0.0
aiortc>=1.6.0
aioice>=0.9.0
av>=10.0.0